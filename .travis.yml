os: linux
language: python
dist: focal
stage: Run premium VM build
group: edge
name: Premium VMs builds
python: '3.9'
arch: amd64
vm:
  size: large
script:
- |-
  df -h
  lspci
  echo "Going for sleep"
  for ((i=1; i<=1; i++)); do
    echo "Elapsed minutes: $i"
    sleep 60s
  done
